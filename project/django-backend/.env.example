# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174,http://127.0.0.1:3000,http://127.0.0.1:5173

# Database Configuration
# Options: 'postgresql' or 'sqlite3'
DB_ENGINE=postgresql

# PostgreSQL Settings (only required if DB_ENGINE=postgresql)
DB_NAME=lms
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# MongoDB Settings (optional - for media and content storage)
MONGODB_ENABLED=True
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=lms
